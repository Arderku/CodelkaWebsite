import { Lexend } from 'next/font/google'
 
export const lexend = Lexend({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400', '500', '600', '700', '800', '900'], // Including all weights up to black (900)
})

